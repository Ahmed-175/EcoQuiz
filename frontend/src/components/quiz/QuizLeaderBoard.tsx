import React from 'react'
import type { IQuiz } from '../../types/types';


type QuizActionsProps = {
  quiz: IQuiz;
};

const QuizLeaderBoard = ({quiz}:QuizActionsProps) => {
  return (
    <div>QuizLeaderBoard</div>
  )
}

export default QuizLeaderBoard